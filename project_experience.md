# 项目经历 — 基于改进 YOLOv8 的智能书籍检测系统

## 项目简介

针对小样本场景下的书籍目标检测任务，基于 YOLOv8 基线算法进行多维度改进优化，设计并实现了融合损失函数优化、空间金字塔池化改进的增强检测模型，并搭建了基于 Gradio 的 Web 端智能检测系统。最终模型在 350 张训练数据上 mAP50 达到 0.925，较基线提升 2%。

## 技术栈

Python | PyTorch | Ultralytics YOLOv8 | Gradio | OpenCV | NumPy | CUDA

## 主要工作

### 1. 算法改进与实验验证

- 分析了 YOLOv8 基线算法在书籍检测场景中的不足，针对小样本条件下的收敛效率与定位精度问题，设计了三个改进方向：损失函数优化、空间金字塔池化模块替换、注意力机制引入
- 独立实现了 **SIoU（Scylla IoU）** 与 **DIoU（Distance IoU）** 损失函数的嵌入，修改 BboxLoss 与 v8DetectionLoss 类支持可配置 IoU 类型切换，解决全局变量传递丢失问题
- 实现了 **SimSPPF** 与 **SPPCSPC** 两个替代池化模块，将 SPPF 中的不可导 MaxPool 替换为可学习的 Conv+ReLU 序列，改善梯度流动
- 基于 **Squeeze-and-Excitation（SE）** 注意力机制实现 C2f_SE 模块，增强特征通道间的权重学习；基于深度可分离卷积实现 C2f_Light 轻量化模块，参数量降低 26%
- 完成 **16 组对比 + 消融实验**，系统验证了各改进模块的有效性与适用边界：SIoU 最优（mAP50 +1.9%），SimSPPF 次之（+1.2%），轻量化与复杂注意力模块在小样本场景下效果不佳的结论为算法选型提供了实证依据

### 2. 工程实现

- 构建模块化的实验训练框架（train.py），通过全局变量机制实现 IoU 损失函数的动态切换，通过 `model.load()` 实现自定义 YAML 架构与预训练权重的兼容迁移
- 编写自动化模型评估脚本（evaluate_model.py / evaluate_all_models.py），支持批量评估、结果汇总与 CSV 导出
- 解决了一系列工程问题：Python 3.8 类型注解兼容性、PyTorch 模型属性在多线程训练中的丢失、YOLO 参数校验器对自定义字段的拦截等

### 3. Web 检测系统

- 基于 **Gradio** 框架搭建 Web 端智能书籍检测系统，支持**图像检测**（上传/粘贴）、**视频分析**（逐帧推理 + 结果视频输出）、**模型切换**（5 个改进版本下拉切换）、**置信度阈值调节**
- 检测结果实时可视化（边界框标注 + 置信度 + 数量统计），系统部署在本机浏览器运行，无需联网

## 项目成果

- 最优模型（SIoU）mAP50 达到 **0.925**，mAP50-95 达到 **0.761**，Precision 达到 **0.944**
- 完整实验数据覆盖损失函数、池化模块、注意力机制、轻量化 4 个方向 × 16 组实验
- 交付 Gradio Web 检测系统，支持图片 + 视频输入，多模型切换

## 简历中可用的一句话版本

> 基于 Ultralytics YOLOv8 框架进行目标检测算法改进，独立实现 SIoU/DIoU 损失函数、SimSPPF 池化模块、SE 注意力机制等 6 个改进模块，完成 16 组对比与消融实验，最优模型 mAP50 达 0.925（+2%）；使用 Gradio 搭建 Web 端智能检测系统，支持图像与视频推理。
